name: Generate-Documentation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  docs:
    uses: kcenon/common_system/.github/workflows/doxygen.yml@main
    secrets: inherit
