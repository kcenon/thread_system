set(LIBRARY_NAME thread_base)

file(GLOB THREAD_BASE_HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/*.h)
file(GLOB THREAD_BASE_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

add_library(${LIBRARY_NAME} STATIC ${THREAD_BASE_HEADERS} ${THREAD_BASE_SOURCES})

target_include_directories(${LIBRARY_NAME} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(${LIBRARY_NAME} PUBLIC utilities)